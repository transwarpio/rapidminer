package io.transwarp.midas.constant.midas.params

object WriteToJsonParams {
  val file = "file"
  val mode = "mode"

  val compression = "compression"
  val dateFormat = "dateFormat"
  val timestampFormat = "timestampFormat"

}
